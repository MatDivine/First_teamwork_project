# You need to create a divide function that takes two numbers as input to the function and divides the first number by the second.

def divide(*, a:float, b:float) -> float:
    '''divide `a` by `b` -> `a/b`'''
    return a / b